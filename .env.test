# Test-runner DB credentials (local docker postgres, see docker-compose).
# medusaIntegrationTestRunner creates a temporary database per suite.
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
